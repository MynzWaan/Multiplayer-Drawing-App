To interact with the project, please
- Clone the repository 
- You can run "npm install" to install the listed dependencies in package.json
- Start the applcation using the command node server.js and access through http://localhost:3000
